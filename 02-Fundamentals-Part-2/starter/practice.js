"use strict";

/* function areaOfShape(length, width) {
  return `Area of the shape is ${length * width} meters.`;
}

console.log(areaOfShape(4, 3));

function numberOfAppleSlices(apples) {
  const piecesOfApple = apples * 12;
  return piecesOfApple;
}
function numberOfOrangeSlices(oranges) {
  const piecesOfOrange = oranges * 12;
  return piecesOfOrange;
}

function piecesOfFruit(apples, oranges) {
  return numberOfAppleSlices(apples) + numberOfOrangeSlices(oranges);
}

console.log(piecesOfFruit(1, 2));

// Arrow function
const calcAge3 = (birthYear) => 2037 - birthYear;
const age3 = calcAge3(1991);
console.log(age3);

const appleSlices = (apples) => apples * 6;
const noOfApples = 2;
console.log(
  `With ${noOfApples} apples you get ${appleSlices(noOfApples)} slices.`
);
 */
/* function yearsUntilRetirement(yearOfBirth, retirementAge) {
  const currentAge = 2022 - yearOfBirth;
  const retirementYears = retirementAge - currentAge;
  return `You have ${retirementYears} years left until you retire`;
}

console.log(yearsUntilRetirement(1979, 55));
 */
/* const yearsUntilRetirement = (yearOfBirth, retirementAge) => {
  const currentAge = 2022 - yearOfBirth;
  const retirementYears = retirementAge - currentAge;
  return `You have ${retirementYears} years left until you retire`;
};

console.log(yearsUntilRetirement(1979, 55));
 */
/* const yearsUntilRetirement = function (yearOfBirth, retirementAge) {
  const currentAge = 2022 - yearOfBirth;
  const retirementYears = retirementAge - currentAge;
  return `You have ${retirementYears} years left until you retire`;
};

console.log(yearsUntilRetirement(1979, 55));
 */
